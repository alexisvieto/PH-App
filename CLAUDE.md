@AGENTS.md
@BUILD-PLAYBOOK.md

# Atrio — estado del proyecto

SaaS **multi-tenant** de administración de Propiedad Horizontal (producto de **Nexera**; Ingesoft es solo un cliente, no confundir). Objetivo central: **transparencia y comunicación** con los residentes.

**Stack:** Next.js 16 (App Router, middleware en `src/proxy.ts`, cómputo server-side por defecto) · React 19 · Supabase (@supabase/ssr, Postgres 17, Storage) · Tailwind v4 · @react-pdf/renderer · sonner · lucide. Puerto dev `3200`. Proyecto Supabase: **PH App** (`ojbwkehifrilkygkkhpa`). Repo `github.com/alexisvieto/PH-App` (rama `main`).

**Marca (centralizada en `src/lib/brand.ts`):** en la app la marca es **«Atrio»** a secas (`PRODUCT_NAME="Atrio"`); el «PH» **solo** vive en el dominio web (`atrioph.net`). Identidad visual (guía + assets oficiales en la carpeta `Branding/`): símbolo **«Prisma»** (cubo isométrico azul) + wordmark **«atrio.»** (minúsculas, punto azul) + **Azul Atrio `#2C5BCB`** + slogan **«One app. One community.»**. Marca blanca: el color del tenant (`brand.primary`) entra como **acento**; la identidad Atrio = lenguaje de diseño + Prisma + crédito. Aplicada en login, favicon/PWA/apple-icon (Prisma sobre tile azul), color de UI (`--brand` en `globals.css`) y los PDFs.

## Dos audiencias
- **Staff** (en `/app`): `organization_members` con roles `owner`/`administrador`/`asistente`. Helpers RLS `is_org_member`, `has_org_role`.
- **Residentes propietarios** (en `/portal`): vía `people.user_id`. Helpers RLS `is_unit_resident`/`is_building_resident`/`is_org_resident`. Resolver de login en `src/app/page.tsx` enruta staff→/app, propietario→/portal, nuevo→/onboarding.

El **tenant** = `organization` (tipo administradora | self_managed), con 1..N `buildings`. Branding por-tenant (`brand.ts`). RLS deny-by-default en todas las tablas; operaciones compuestas/atómicas vía RPC `SECURITY DEFINER` (search_path fijo, grant solo `authenticated`, auto-autorizadas). Integridad cross-tenant con **FKs compuestas** `(building_id/unit_id/…, organization_id)`. Dinero = admin-only; padrón/operación = cualquier miembro.

## Construido (todo en `main`, con auditoría 3-agentes + E2E salvo donde se indique)
- **Cimiento (Fase 1):** organizations, profiles, organization_members; padrón: buildings, units (coefficient, **floor/letter/area_m2**, inquilino liviano `is_rented`/`tenant_name`/`tenant_phone`), people, unit_ownerships (historial de ventas, **`is_primary`** = contacto principal), unit_leases (% alquilado), **`unit_amenities`** (estacionamientos/depósitos con ubicación). RPCs atómicos `transfer_ownership`/`register_lease`/`end_lease`. **Módulo Propietarios** (`/app/propietarios`, reemplaza "Personas"): navegación Edificio→Piso→Unidad→Propietario(s) (varios por unidad, sin límite), filtro de alquiladas, gestión de propietarios/estacionamientos/depósitos/inquilino. El responsable financiero es el propietario; el inquilino es dato informativo. Auth + app shell con marca + selector de org activa (cookie).
- **Administrativo financiero (Fase 2):** `fee_settings` (cuota por_coeficiente|monto_fijo), `charges` (incl. multas), `payments`, `expenses`. RPC `generate_monthly_charges` (admin, idempotente) + **auto-generación el día 1 de cada mes** vía `pg_cron` (job `generate-monthly-charges` → `generate_all_monthly_charges()`, función de sistema SECURITY DEFINER sin check de admin, recorre los edificios con `fee_settings`). El texto al residente dice **"Pago a realizar"** (no "saldo pendiente", para no implicar morosidad); el admin sí ve "Saldo pendiente". Estado de cuenta (ledger: saldo = Σcargos−Σpagos) + **PDF con marca**; gastos + reportes ingresos/gastos; **paz y salvo** (PDF si saldo≤0). `lib/statement.ts` reusado por staff y portal.
- **Portal del Residente + Comunicación (Fase 3 — completo, auditado 3-agentes):** `/portal` abre con comunicados (no con la deuda); estado de cuenta read-only + PDF + paz y salvo (solo lo suyo). Tabla `announcements` con `kind` (anuncio|novedad); **acuse de visto** (`announcement_reads`): admin ve "Visto por N" + quién/cuándo. Auto-vínculo `people.user_id` por email **solo con correo confirmado** (trigger). **Flyer para áreas sociales** (PDF con marca por comunicado) y **reporte a la Junta Directiva** (`/app/reportes` → PDF que agrega novedades + quejas + anomalías por rango de fechas).
- **Quejas/Solicitudes + Sanciones (Fase 4 — completo, auditado 3-agentes):** `tickets` + `ticket_messages` (hilo bidireccional; RPC `create_ticket`; staff gestiona estado). **Multas y llamados de atención** (`infractions`, FKs compuestas): RPC atómico `create_infraction` — la multa exige admin y **genera el cargo** (`concept='multa'`) vinculado que aparece en el estado de cuenta; el llamado de atención va sin monto. UI `/app/sanciones` (lista + alta) + **formato PDF** con marca por sanción.
- **RRHH / Nómina (Fase 6) — motor multi-país + UI (parcial):** Constantes legales como **datos versionados** (no hardcodeadas). Catálogo global `countries`/`legal_rule_sets`/`contribution_rates`/`tax_brackets`/`legal_constants` (lectura autenticada) + **paquete Panamá v1** sembrado. RRHH del tenant (`employees`, `salary_history`, `payroll_periods`, `payroll_items`, `payroll_incidences`, `liquidations`) **admin-only**, FKs compuestas. Motor puro en `lib/payroll/` (`loadRuleSet`, `computePayroll`, `computeLiquidation`) — el cálculo lee el paquete del país del empleado y guarda `rule_set_id` (reproducible). UI `/app/rrhh`: alta de empleados + **calculadora de planilla** (bruto→CSS/seg.educativo/ISR→neto) y **calculadora de liquidación** (matriz renuncia/mutuo/despido + derechos adquiridos). N/A vía flag `applies` por concepto. La ficha incluye **datos personales** (fecha de nacimiento → **edad calculada en vivo**, sexo, dirección, teléfono, correo, contacto de emergencia, N° Seguro Social, banco/cuenta), **foto y contrato** (`photo_path`/`contract_path`) en bucket privado **`ph-docs`** (imágenes+PDF, 20MB, RLS por carpeta=org **admin-only**; subida cliente→Storage + signed URLs; reemplazo borra el anterior), e **historial de amonestaciones** (`employee_warnings`: fecha + causa + tipo verbal/escrita/suspensión + documento opcional). **Editar empleado** + **cambio de salario** (registra en `salary_history`); **dar de baja/reactivar** (`status`/`termination_reason`/`termination_date`); **alerta de vencimiento de contrato** (`contract_end_date` vía `maintenanceAlert`); avatar en el listado. Campos compartidos alta/edición en `employee-form-fields.tsx`. **Planilla por período** (`/app/planilla`): `payroll_periods.kind` (ordinaria|xiii) + `payroll_items`; corrida en lote de empleados activos (server action `processPayrollPeriod`, RPC-libre pero inserción atómica), estados borrador→procesada→pagada, detalle con renglones+totales. **XIII mes** por partida (ventana del cuatrimestre, CSS especial 7.25%, sin seg. educativo; ISR proporcional pendiente de revisión). `computePeriodPayroll`/`computeXiii` en `lib/payroll/engine.ts`. **PDFs profesionales** (kit reutilizable `components/pdf/_kit.tsx`: header con marca + footer con paginación y crédito Nexera): **recibo de pago** por renglón (ordinaria y XIII) y **carta de liquidación**. Crédito `VENDOR_CREDIT = "by Nexera · www.nexerai.io · ventas@nexerai.io"` (se muestra si `export_credit`). **Verificado E2E (Playwright):** base $2,000 → neto $1,592.50; liquidación 7 años despido = $16,557.71. **Pendiente:** planilla por período (batch), PDFs (recibo/carta), QR conserjes/asistencia/fichas, auditoría 3-agentes.
- **Mantenimiento + Inventario (Fase 5) — completo (datos + UI, auditado 3-agentes):** `equipment` (inventario, cronograma vía `next_maintenance` + trigger de recálculo, alertas próximo/vencido), `maintenance_logs` (historial), `anomaly_reports` + `anomaly_photos` (1..N, FK compuesta `(anomaly_id, organization_id)`), `suppliers` (catálogo). **Storage** bucket privado `ph-photos` (RLS por carpeta = org, 10MB, jpeg/png/webp). Staff-only en v1. UI: `/app/proveedores` (CRUD), `/app/mantenimiento` (+detalle con historial y registro), `/app/anomalias` (+detalle con galería vía **signed URLs**). Subida de fotos orquestada en cliente (crear → upload → `recordAnomalyPhotos` con validación de ruta `{org}/anomalias/{id}/`). **Deuda:** E2E real de subida de foto (requiere navegador).

- **Accesos y Seguridad (Fase 9a — Staff + Garita + Portal, completo) — MÓDULO PAGO:** add-on que Nexera activa por org. **Entitlement**: tabla `organization_modules` (org, módulo, activo) + helper `has_module(org,'accesos')`; el nav y las rutas se ocultan/bloquean si no está activo (la RLS de las tablas de accesos también lo exige). Rol **`guardia`** en `org_role` (encerrado en garita/paquetería). `visitor_passes` (pases de visita: tipo, vigencia, días/horario recurrente, código+QR, usos, placa; **tipo `indefinido`** sin vencimiento —`valid_to` nullable, p. ej. doméstica con contrato indefinido— que exige **nombre + cédula/pasaporte** como registro formal; el RPC de garita ya es seguro porque `fecha > NULL` es falso) y `visitor_log` (bitácora entrada/salida con foto/placa). UI `/app/accesos` (staff: crear pase + lista + bitácora; detalle con **QR** `qrcode` server-side + WhatsApp + anular), **garita** (el guardia valida código/QR y registra entrada/salida; escáner QR nativo en la app) y **portal** (`/portal/accesos`: el residente crea pases para su(s) unidad(es), RLS `is_unit_resident`). Empaquetado **PWA→Capacitor** modo "shell" (base lista; `capacitor.config.ts` appId `io.nexerai.atrioph`).
- **Botón de pánico (Fase 9, completo):** `panic_alerts` — SOS **residente→garita** y **guardia→admin**, activación **manteniendo presionado 2s**, un botón con tipo de emergencia opcional. Entrega por Supabase **Realtime** con **fallback de polling** (la entrega del lado residente por Realtime es poco fiable). Auditado 3-agentes.
- **Reservas + Comunidad (Fase 8, en progreso):** **Reservas de áreas** (`common_areas` + `area_reservations`): calendario con días **ocupados (gris)** vs **disponibles (azul claro)** + horario reservado; el **admin también reserva** y puede **bloquear** un área sin unidad (mantenimiento/evento del edificio); **carrusel** de recursos reservables (salón, elevador, mudanza, BBQ) con iconos de color (no tarjetas grandes). **Límite de anticipación** = `common_areas.advance_days` (por área; default **90 días = 3 meses**; lo valida `createReservation`). **Bloqueo por morosidad** (2026-06-25): un residente con **2+ meses de cuota de mantenimiento vencidos** (`getUnitOverdueMonths`, FIFO de pagos sobre las cuotas, `MAX_OVERDUE_MONTHS`) **no puede reservar** — el portal oculta el calendario y muestra "comunícate con la administración"; **enforced en servidor** (`createResidentReservation`), no solo en la UI. El staff no se bloquea. **Reglamento + aceptación** (2026-06-25): cada área tiene `common_areas.rules` (reglamento con multas; sembrado un **genérico de muestra**, editable; el form de área lo trae pre-llenado con `GENERIC_AREA_RULES`). Al reservar (residente, prop `requireRules`) se muestra el reglamento + **checkbox obligatorio** (`RULES_ACCEPT_LEGEND`, botón deshabilitado hasta marcar) y la reserva guarda **congelados** `area_reservations.rules_accepted_at` + `rules_snapshot` (la "firma" del documento); visible en "Mis reservas". **Dashboard de reservas** (2026-06-25): `/app/reservas/dashboard` (botón "Generar Dashboard" en `/app/reservas`) — gráfico de **barras** de cantidad de reservas por área (activas: aprobada+pendiente), **seleccionable por mes/año**, dinámico (todas las áreas), ordenado por uso ("área más usada"); **marca Atrio** (lockup arriba-izquierda, barras en `bg-brand`). **Votaciones** (`votations`/`votation_options`/`votation_votes`): **POR UNIDAD (Ley 284, realineado 2026-06-25)** — 1 unidad **al día** = 1 voto; el electorado (denominador de quórum y aprobación) = **unidades al día** del edificio (`votation_eligible_units`); un propietario con varias unidades **vota una boleta por cada unidad al día**; el **moroso** (cualquier cuota de mantenimiento vencida, `unit_overdue_maint_count`>0) **no ve** la votación ni recibe boleta. `cast_vote(votación, **unidad**, opción, abstención)` valida titularidad (propietario principal) + ventana + al día; peso=1. El portal arma las boletas con `votable_units_for` (unidades del residente con bandera al día). Escrutinio `tally` por conteo de unidades; **aprobación = Sí / unidades al día ≥ umbral** (el ausente cuenta como no-favorable). **acta de votación** en PDF (por unidad, sin coeficiente). El coeficiente queda solo para cuotas/gastos. Ver [[project_ley284-votaciones]]. **Ciclo dirigido por FECHAS** (2026-06-25): `opens_at`/`closes_at` obligatorios; la fase (programada/abierta/cerrada) se **deriva de las fechas** (helper `votationPhase`), **sin abrir/cerrar/cancelar manual** (se quitó `VotationAdminControls`/`setVotationStatus`). `cast_vote` valida la ventana por fechas; el **acta solo existe cuando ya cerró** y abre como PDF (enlace `target=_blank`, no menú de compartir) en portal y admin. Cron `sync-votation-status` mantiene el `status` persistido coherente. **Acta INMUTABLE** (2026-06-25): al cerrar se **congela** `votations.result_snapshot` (total de coeficientes, votos con su peso, opciones y umbrales) vía `freeze_votation_result` (write-once, SECURITY DEFINER, congela `eligible_units`+votos+opciones+umbrales; también la corre el cron y la ruta del acta al abrirla); `loadVotationResults` y `renderActaPdf` se arman de esa foto, así el acta es idéntica para siempre aunque cambien las unidades al día. Una votación cerrada **no se puede editar** (RLS `vot_update` bloquea si `now() >= closes_at`) **ni borrar** (sin política DELETE). Verificado: al subir un coeficiente el total vivo pasó a 1.5 pero el acta siguió en 1.0. **«A domicilio»** (`providers`, directorio **GLOBAL** curado por Nexera, sin tenant: categoría, logo, link, WhatsApp): sección estilo "apps de comercios" para los residentes; nada que ver con el PH (Nexera los agrega por contratos).
- **Centro de notificaciones del staff (campanita, 2026-06-26):** tabla `notifications` (org, type, title, body, link, source_type/id, read_at) con RLS staff (`is_org_member` lee y marca leído; nadie inserta por API). **Disparadores** SECURITY DEFINER (revocados de anon/authenticated) la alimentan cuando un **residente** —no el staff, vía `notif_is_staff`— crea una **reserva pendiente**, una **queja** nueva, una **respuesta** en un hilo de queja, o se registra un **pago**. **NO** incluye pánico (tiene su canal SOS dedicado). UI: bandeja con **leído/no-leído** en el app-shell del admin — `NotificationsProvider` (contexto único, polling ~25s + al volver el foco) + `NotificationBell` (contador, marcar una/todas, **sonido** Web Audio al llegar con **toggle de silencio** en localStorage; `align` para abrir bien en sidebar/escritorio vs barra móvil; oculta para el guardia). **Reserva del residente:** al enviar **cierra el formulario** + `createResidentReservation` hace `revalidatePath` + `AutoRefresh` (foco + 30s) → la reserva y su aprobación aparecen sin recargar.
- **Portal del residente — rediseño a TABLERO + HUBS (2026-06-26):** el home dejó de ser un grid de ~10 tiles (no escalaba; cada módulo nuevo = otro ícono). Ahora el home es un **tablero de atención**: hero de saldo + **tarjetas que solo aparecen cuando aplican** (`AttentionCard`: visita en garita, paquete, votación abierta) + SOS + comunicados. La navegación son **5 hubs temáticos** en la barra inferior (ahora **siempre visible**, no solo móvil): **Inicio · Cuenta · Accesos · Comunidad · Más** (`portal-shell` con `tabFor()` que ilumina el hub aunque entres a una sub-función). Cada hub es una **lista de `HubRow`** (ícono tintado + label + sub + chevron) con **color por grupo** (Cuenta=esmeralda, Comunidad=violeta, Accesos=índigo+chips, Más=pizarra; rojo solo urgencias). Componentes en `components/portal/hub.tsx` (`HubRow`/`AttentionCard`/`HubHeader`/`HubSection`); páginas `portal/{cuenta,comunidad,mas}` + `accesos` como hub. Íconos lucide más representativos (ReceiptText, CalendarCheck, Package, MessageSquareWarning…). **Un módulo nuevo entra en su hub, nunca en el home.** Apoyado en la skill **ui-ux-pro-max** (táctil ≥44px, jerarquía h1→h2, sin emojis como íconos). **SOS y Paquetes persistentes en el header** del portal (alcanzables desde cualquier hub; el SOS es emergencia, Paquetes pasó de `Bell` a `Package`). **Alertas al residente:** `PackageWatcher` (en `portal-shell`, sondeo ~30s + al volver el foco) refresca el badge/tarjeta de paquetes y **suena + vibra** cuando **llega un paquete nuevo** (el conteo sube). Util reutilizable `lib/alert-sound.ts` (`playChime` con `AudioContext` singleton + `shortVibrate`), que **también usa la campanita del admin** (se unificó). El citófono ya era realtime (`IntercomListener`). **SOS/pánico → alarma (2026-06-26):** `PanicListener` (staff, montado en todo `/app`, realtime) ahora dispara **`playAlarm`** (bips cuadrados alternos C6↔G5, estridente, ~1.7s) + **vibración fuerte** (`shortVibrate([300,150,300,150,300])`) además del toast urgente; sin silenciar (es emergencia). `playAlarm` vive en `lib/alert-sound.ts` junto a `playChime`. **SOS con respaldo de polling (2026-06-26):** el `PanicListener` combina realtime + **sondeo cada 12s** (dedupe por id, no alarma por alertas ya activas al abrir) — con solo realtime el SOS no sonaba (poco fiable). **Notificaciones de queja (2026-06-26):** una queja = **UNA** notificación (el primer mensaje que abre el ticket ya NO notifica aparte; `create_ticket` inserta ticket + mensaje, antes disparaba 2); al **resolver/cerrar** la queja —o aprobar/rechazar/cancelar una reserva pendiente— sus notificaciones se marcan **leídas** (la campanita se limpia sola) vía `trg_notif_ticket_resolved` / `trg_notif_reservation_handled`.
- **Sidebar del admin agrupado (2026-06-26):** los ~21 módulos del `app-shell` se reparten en **secciones siempre visibles** (con título, no plegables): **Administración** (Edificios, Propietarios, Contabilidad, Planilla, RRHH, Reportes) · **Operaciones** (Mantenimiento, Anomalías, Proveedores, Proyectos) · **Seguridad y accesos** (módulo pago: Accesos, Garita, Paquetería, Emergencias) · **Servicios al propietario** (Comunicados, Reservas, Votaciones, Quejas, Sanciones). **Inicio** fijo arriba, **Configuración** fija abajo (junto al usuario/Salir). `NAV_GROUPS` + helper `isVisible` (gating por módulo activo y rol `guardia`); una sección se **oculta** si no tiene ítems visibles (p. ej. el guardia solo ve Inicio + Garita/Paquetería). Un módulo nuevo entra en su grupo, el sidebar no se alarga. Mismo árbol en el drawer móvil.
- **Directorio de Proveedores para el hogar (módulo del residente — "la joya", 2026-06-26):** GLOBAL (sin organización, curado por Nexera), **distinto** del «A domicilio» (`providers`, comercios) y del «Proveedores»/`suppliers` del admin (mantenimiento por tenant). Tablas: `service_categories` (11 sembradas — Electricidad, Construcción, Electrodomésticos, Remodelación, Diseño de Interiores, CCTV, Alarmas, Electrónica, Cerrajería, Carpintero, Cocina; Nexera agrega sin programar), `service_providers` (logo, contacto, tel/WhatsApp/correo, `priority` = exposición de quien paga más, `rating_avg`/`rating_count` **cacheados**), `service_provider_categories` (M:N, varias categorías por proveedor), `service_provider_reviews` (UNA por residente, editable; estrellas 1-5 + texto + foto opcional; **trigger `trg_provider_rating` recalcula el promedio** al insert/update/delete). RLS: **todos los residentes de cualquier org leen lo mismo**; solo Nexera (`is_platform_admin`) crea/edita proveedores y categorías; cada residente solo su reseña. **Cobro por fuera** (Nexera; en el app solo el campo de prioridad). UI residente: `/portal/proveedores` (tablero tipo iOS, chips por categoría + estrellas, estilo «A domicilio») + detalle `/portal/proveedores/[id]` (Llamar/WhatsApp/Correo + reseñas estilo Amazon + `ReviewForm`/`Stars` + `submitReview` upsert). **Protagonista:** tarjeta destacada en Inicio + fila en hub Comunidad. **Verificación "fue contratado":** v1 abierto (reseña por residente real Atrio + moderación de Nexera); **pendiente** la capa de *contact-gating* (registrar el contacto → solo quien contactó por Atrio califica → de paso da leads a Nexera). **Pendiente:** subida de logo (Nexera) y foto de reseña (bucket `directorio`), y la subida de foto de reseña.
- **Panel de plataforma Nexera (`/admin`, super-admin cross-tenant, 2026-06-26):** guard `getPlatformAdmin`/`platform_admins`. Nav: **Suscriptores · Proveedores · Publicidad**. **Suscriptores** (`/admin/suscriptores`): lista de orgs + **alta de suscriptor** (org + su primer usuario admin dueño) + **agregar más admins**, vía RPCs SECURITY DEFINER gated por `is_platform_admin`: `platform_create_org` (org sin volver dueño al llamador), `platform_create_org_admin` (**crea el usuario de auth** en `auth.users`+`auth.identities`+`profiles`+`organization_members`, **espejando GoTrue** — tokens `''` no NULL, identity con sub/email, contraseña **bcrypt** vía `crypt`/`gen_salt`; **sin service-role key**, que no hay en el env), `platform_onboard_subscriber` (ambos en un paso). **Proveedores** (`/admin/proveedores`): CRUD del directorio global (alta con categorías M:N por checkbox, prioridad, activar/desactivar). RLS: policies de lectura de `organizations`/`organization_members` para `is_platform_admin`. **Verificado E2E:** Nexera crea un suscriptor → el nuevo admin **inicia sesión y llega a /app**. La contraseña es **temporal** y se le **fuerza a cambiarla en el primer login**: `platform_create_org_admin` marca `profiles.must_change_password=true`; `getSessionContext` lo expone y el resolver + el layout de `/app` redirigen a **`/cambiar-clave`** hasta que el admin define su contraseña definitiva (`supabase.auth.updateUser` + RPC `mark_password_changed`). Verificado E2E: login con clave temporal → `/cambiar-clave` → tras cambiarla entra a `/app`; re-login con la nueva ya no la fuerza. **Módulos por organización (2026-06-26):** en Suscriptores, Nexera **activa/desactiva los add-ons pagos por suscriptor** (hoy `accesos`; catálogo `PLATFORM_MODULES` en la página) con un toggle, vía `platform_set_org_module` (SECURITY DEFINER, gated, upsert en `organization_modules`); el nav y las rutas del tenant lo respetan con `has_module` (+ policy de lectura de `organization_modules` para `is_platform_admin`). Verificado E2E: apagar/encender el módulo desde el panel cambia `organization_modules.enabled`. **Auditoría 3-agentes (2026-06-26):** sin críticos/altos en el panel (los RPCs validan `is_platform_admin` antes de tocar nada, **solo crean**, no dañan tenants; creación de usuario robusta para GoTrue). Endurecido: la tabla raíz `platform_admins` **sin grants de escritura** a anon/authenticated; RPCs `platform_*` **revocados de PUBLIC** (solo `authenticated` que se auto-autoriza); **morosidad de reservas enforced en BD** (trigger `chk_reservation_overdue` con `unit_overdue_maint_count>=2`, antes solo en el server action → había bypass por API directo); `freeze_votation_result` con check de org **cron-safe**; los 2 triggers de notificación al resolver ahora **best-effort** (`EXCEPTION`); gating de módulo en `/app/accesos/[passId]` + chequeo de dueño de unidad en estado de cuenta/PDF/paz-y-salvo del portal (defensa en profundidad sobre RLS). Diferido: FK `reviews.user_id→auth.users` (el seed del demo usa user_ids ficticios), atomicidad en alta de proveedor/campaña (solo Nexera).
- **Gestión de usuarios del tenant + compuerta de registro (2026-06-26):** **Residentes** se autoregistran en el login (`auth.signUp`, modo "Crear una") **solo si su correo ya está en el padrón** (`people.email`) — enforced en el servidor por trigger **`trg_enforce_signup_allowlist`** (BEFORE INSERT en `auth.users`; raise si el correo no está en `people`) + pre-chequeo `email_is_registered` en la pantalla para un mensaje claro. Las creaciones controladas hacen **bypass** marcando `set_config('app.bypass_signup_gate','on',true)` (lo usan `platform_create_org_admin` y `org_create_member`). **Staff** (administrador/asistente/guardia): el admin del PH los crea en **Configuración → "Equipo del PH"** vía RPC **`org_create_member`** (gated `has_org_role(owner|administrador)`, contraseña temporal + `must_change_password`); la opción "guardia" solo aparece si el módulo accesos está activo. El **admin de plataforma (Nexera)** sin organización cae directo en `/admin` (resolver). **Verificado E2E:** correo fuera del padrón no puede registrarse (0 cuentas creadas); el admin crea un guardia → entra y es forzado a `/cambiar-clave`.
- **Proyectos / Transparencia del gasto (Fase 8, 2026-06-25):** `projects` (título, descripción, status abierto|adjudicado|cerrado, `award_reason`, `awarded_at`) + `project_quotes` (empresa, monto, nota, `file_path`, `is_winner`; FK compuesta). La administración crea un proyecto (p. ej. "Pintura de PH"), sube cotizaciones (con PDF/imagen opcional) **visibles a los residentes**, y **adjudica la ganadora con una justificación** (campo "por qué se seleccionó") vía RPC atómico `award_project_quote`. RLS: staff + residentes leen (`is_org_resident`), escritura admin-only. Bucket privado **`ph-proyectos`** (PDF+imagen, 20MB, lectura org incl. residentes, escritura admin). UI `/app/proyectos` (lista + alta + detalle con cotizaciones/adjudicar/borrar) y `/portal/proyectos` (solo lectura: cotizaciones + ganadora + justificación). Verificado **E2E** (admin crea+cotiza+adjudica, residente lo ve). Ícono `HardHat`. Al adjudicar, **registra el gasto en Finanzas** vinculado al proyecto (`expenses.project_id`; el RPC inserta el gasto, idempotente).
- **Transparencia financiera al residente (2026-06-25):** vista `/portal/finanzas` (tile en la home) — el residente ve **ingresos (recaudado) vs egresos** del mes + **balance**, con **detalle de gastos** por categoría/descripción y la **nómina como UN solo total** ("Personal / nómina", sin sueldos individuales — privacidad). Vía RPC de agregados `get_finance_summary(org, from, to)` (SECURITY DEFINER, autorizado a `is_org_resident`/`is_org_staff`; no expone registros individuales ni cambia RLS sensible). Lo demás financiero sigue admin-only (cargos/pagos por unidad solo los suyos; planilla detallada solo admin).
- **Contabilidad — ciclo contable completo (2026-06-25):** Atrio como sistema de registro contable (NIIF + Ley 284). **Catálogo de cuentas** (`ledger_accounts`) estándar de 5 dígitos sembrado por tenant (activos/pasivos/patrimonio/ingresos/gastos, fondos operativo/imprevistos, `system_role` para los auto-asientos, flags fiscales), editable + crear cuentas. **Partida doble** (`journal_entries`+`journal_lines`, check one-side, FKs compuestas) con **auto-posting por TRIGGERS**: cada cuota/pago/gasto/multa/recargo/planilla genera su asiento balanceado (SECURITY DEFINER, idempotentes por source, **revocadas de anon/authenticated** — solo las invocan los triggers). **Fondo de Imprevistos**: apropiación automática del % configurado en cada pago. **Motor de recargos** por morosidad: `generate_late_fees()` (cron diario) crea el cargo "Recargo por morosidad X%" automático en el estado de cuenta (% configurable por edificio 10–20% + día de corte en `fee_settings`). RPC `accounting_summary`. UI `/app/contabilidad` (Estado de Resultados + Balance General + chequeo de cuadre + **Informe mensual PDF**) y `/app/contabilidad/cuentas` (catálogo) y **`/app/contabilidad/gastos`** (Pagos a proveedores: escoge del catálogo `suppliers` vía datalist, registra el gasto y su asiento). **Recibo/constancia de pago en PDF por cada pago** (`/recibo/pago/[paymentId]`, kit corporativo, RLS staff+residente de la unidad), enlazado en el estado de cuenta (staff y portal, escritorio y acordeón móvil). **Planilla en DOS pasos** (2026-06-25): al **procesar** se causa el gasto (DR Gasto de personal / CR **Sueldos por pagar** `20150` `nomina_por_pagar`); al **"Registrar pago"** (desembolso, antes "Marcar pagada") sale del banco (DR Sueldos por pagar / CR Banco) → el botón es el verdadero "generar el pago". `post_payroll_period` idempotente: `payroll`=causación (reprocesable), `payroll_pago`=desembolso; al borrar el período se quitan ambos. **Comprobante de pago de planilla** en PDF (`/app/planilla/[id]/comprobante`). Nota: los **pagos a proveedores** (gastos) se registran como pagados al momento (DR gasto / CR banco) — el registro ES el desembolso. **Auto-generación de quincenas** (2026-06-25): al entrar a `/app/planilla`, las quincenas vencidas (el **15 y el último día**, según la frecuencia de cada empleado activo) se **crean y procesan solas** (`ensureDuePayrollPeriods` en `lib/payroll/run.ts`, idempotente, ventana ~35 días, índice único `payroll_periods_ordinaria_uq` por org+frecuencia+inicio) → el admin solo **revisa y autoriza el pago**, no captura nada. `runPayrollPeriod` (núcleo de cálculo) lo reusan la acción `processPayrollPeriod` y la auto-generación. (Genera al abrir la página; un cron a medianoche es opción futura — requiere endpoint + secreto.) Config de recargo/reserva en la cuota del edificio. **Verificado: el libro CUADRA** (descuadre 0; Activos = Pasivos + Patrimonio). **Pendiente (ver PENDIENTES):** presupuesto vs real (Art. 94), libro mayor/diario drill-down, conciliación bancaria, exportables DGI (Form. 930 / Informe 43), ITBMS a proveedores + retención, QR de verificación en paz y salvo; los post_* son AFTER INSERT (un cargo/pago borrado deja el asiento — falta sync en update/delete).
- **Marca Atrio + PDFs corporativos (2026-06-24):** identidad definitiva aplicada en todo (ver bloque «Marca» arriba). **Kit de PDF reutilizable** `components/pdf/_kit.tsx` (`AtrioPrisma`, `AtrioLockup`, `BrandHeader`, `Subject`, `Footer`, `pdfStyles`): **los 9 PDFs** (estado de cuenta, paz y salvo, reporte JD, sanción, flyer, acta, recibo de pago, liquidación) con header = **lockup Atrio + nombre del edificio debajo**, look corporativo y sobrio (sin banda de color; total en caja de contorno; pie con cubo + crédito). Los exportables se pueden **ver antes de compartir**.

## Lecciones (bugs que tsc/lint NO detectan — solo runtime/E2E)
- Archivos `"use server"` **solo exportan funciones async**; constantes/objetos de estado van en `src/lib/action-state.ts`.
- Nada de `setState` en `useEffect`: usar `components/use-form-panel.ts` (ajuste-en-render con guard).
- Las **FKs compuestas** vuelven **ambiguos** los embeds de PostgREST a esa tabla → usar hint `tabla!fk_name(...)` o mapear aparte (no embeber).
- Crear usuarios de prueba por SQL: normalizar columnas de token de `auth.users` a `''` (no NULL) o GoTrue falla.
- **@react-pdf/renderer:** no carga SVG externos como `<Image>` → portar los paths a primitivas `Svg/Path` (así está `AtrioPrisma`). Un `<Text>` con fontSize grande **no reserva su altura de línea** y el siguiente texto se encima → fijar `lineHeight: 1` + márgenes. Verificar PDFs **viéndolos**: rasterizar a PNG con `pdf-to-png-converter` (dev-dep de un solo uso) + Playwright logueado, y mirar la imagen.
- **Supabase Realtime** del lado residente (RLS `is_unit_resident`) es **poco fiable** → patrón de **fallback con polling** (se usó en el botón de pánico).

## Pendientes y siguiente paso
Ver **`PENDIENTES.md`** (contexto vivo) y **`PLAN-DE-TRABAJO.md`** (roadmap). Fases 3, 4, 5 y 9a cerradas y auditadas; marca Atrio + PDFs corporativos aplicados. **Pendientes:** RRHH/planilla por período (cerrar y auditar 3-agentes), pagos en línea (brecha vs Munily), empaquetado final a las tiendas (cuentas Apple/Google) y el sitio web `atrioph.net`. Las credenciales del demo siguen en `@modusph.app` (migrar cuando se decida).
